# Islamic Text Validator for AI RAG Systems

A blazingly fast, scale-to-zero microservice designed to fact-check, score, and correct Quran and Hadith text generated by Large Language Models (LLMs).

When building an Islamic RAG (Retrieval-Augmented Generation) system, LLMs often hallucinate or subtly misquote sacred texts. This validator acts as a firewall. Instead of relying on heavy, slow vector embeddings for exact fact-checking, it uses an embedded SQLite database with highly optimized BM25 lexical search and mathematical string alignment to verify text with absolute accuracy.

## Key Features

- **Zero Database Latency:** Uses a read-only, embedded SQLite database. No external database connections, no network latency, and zero persistent hosting costs.
- **BM25 Lexical Search:** Utilizes SQLite FTS5 to find exact keyword matches for LLM hallucinations, bypassing the "fuzzy" conceptual matches of vector databases.
- **Mathematical Scoring:** Calculates Jaro-Winkler similarity on normalized text to return a strict correctness score for the generated text.
- **Bilingual Support:** Maps both English and Indonesian translations alongside the original Uthmani Arabic text.
- **Scale-to-Zero:** Designed for Google Cloud Run. The immutable data footprint allows for instant cold starts and infinite horizontal scaling.

## Architecture

```
[ Incoming Request ] → [ Cloud Run Container ] → [ Local Go App Memory ] → [ Embedded SQLite FTS5 ]
                                                                                  (No Network Latency)
```

The system is split into two distinct parts:

1. **The Seeder (`cmd/seed`):** A build-time CLI that parses raw JSON datasets from GitHub, aligns English and Indonesian translations, and generates a compressed `data.db` SQLite file.
2. **The Validator API (`cmd/validator-api`):** The runtime HTTP server that receives text from your RAG system, queries the embedded `data.db`, scores candidates, and returns ranked canonical matches.

Compared to a MongoDB + vector DB setup for validation, an embedded SQLite + pure Go stack changes operational characteristics completely:


|                 | Embedded SQLite + Pure Go                             | MongoDB + Vector DB                  |
| --------------- | ----------------------------------------------------- | ------------------------------------ |
| **Latency**     | Local microsecond CPU lookups                         | Network-bound milliseconds           |
| **Cost**        | Pay-per-request (scale to zero)                       | Persistent monthly cluster fees      |
| **Maintenance** | Versioned with code; no backups or connection pooling | Backups, pooling, indexing to manage |


## Tech Stack

### Core Application


| Component           | Choice                         | Rationale                                                                                         |
| ------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------- |
| **Language**        | Go (Golang 1.25+)              | Single static binary, small memory footprint, fast compile, millisecond cold starts on Cloud Run. |
| **Database Engine** | SQLite 3 with FTS5             | Embedded read-only file in the container image; FTS5 provides BM25 lexical search out of the box. |
| **Database Driver** | `modernc.org/sqlite` (pure Go) | CGO-free driver enables fully static cross-compilation and a lean, secure container image.        |


### Validation & Scoring


| Component             | Choice                                   | Rationale                                                                                                   |
| --------------------- | ---------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| **String similarity** | `github.com/adrg/strutil` (Jaro-Winkler) | After FTS5 narrows candidates, exact mathematical string matching runs in microseconds with negligible RAM. |


### Infrastructure & Deployment


| Component            | Choice                     | Rationale                                                                                                         |
| -------------------- | -------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| **Hosting**          | Google Cloud Run           | Fully managed, scale-to-zero when idle, horizontal scaling with no external DB bottlenecks.                       |
| **Containerization** | Docker (multi-stage build) | Compile in a Go builder stage, ship only the binary and `data.db` in a minimal `distroless/static` runtime image. |
| **CI/CD**            | GitHub Actions             | Downloads JSON, runs the seeder, builds the image, and deploys to Google Artifact Registry / Cloud Run.           |


## Project Structure

```text
.
├── .github/
│   └── workflows/
│       └── deploy.yml          # CI/CD: download JSON, run seeder, deploy to Cloud Run
├── cmd/
│   ├── seed/
│   │   └── main.go             # CLI that parses JSON and builds data.db
│   └── validator-api/
│       └── main.go             # HTTP server for Cloud Run
├── internal/
│   ├── database/
│   │   ├── sqlite.go           # SQLite connection logic
│   │   └── queries.go          # FTS5 SQL queries (BM25 search, direct lookup)
│   ├── models/
│   │   ├── json.go             # Structs for unmarshaling raw GitHub JSON
│   │   └── entity.go           # Core structs (Quran, Hadith, API payloads)
│   └── validator/
│       ├── scoring.go          # Jaro-Winkler normalization and similarity
│       └── service.go          # Business logic combining search and scoring
├── data/
│   ├── raw/                    # (gitignored) Temporary folder for downloaded JSON files
│   └── generated/              # (gitignored) Destination for the compiled data.db
├── Dockerfile                  # Multi-stage Docker build
├── go.mod
└── go.sum
```

Data source:

[https://github.com/fawazahmed0/quran-api](https://github.com/fawazahmed0/quran-api)

### Quran

- **Quran Academy (Standard):** `https://cdn.jsdelivr.net/gh/fawazahmed0/quran-api@1/editions/ara-quranacademy.json`
- **Indo-Pak Script (Common in South Asia):** `https://cdn.jsdelivr.net/gh/fawazahmed0/quran-api@1/editions/ara-quranindopak.json` 
- **Uthmani Script / Encoded:** `https://cdn.jsdelivr.net/gh/fawazahmed0/quran-api@1/editions/ara-quranuthmanienc.json`
- **Ind translation:** [https://cdn.jsdelivr.net/gh/fawazahmed0/quran-api@1/editions/ind-indonesianislam.json](https://cdn.jsdelivr.net/gh/fawazahmed0/quran-api@1/editions/ind-indonesianislam.json)
- **Eng Translation:** [https://cdn.jsdelivr.net/gh/fawazahmed0/quran-api@1/editions/eng-ummmuhammad.json](https://cdn.jsdelivr.net/gh/fawazahmed0/quran-api@1/editions/eng-ummmuhammad.json)
- **Eng Translation:** [https://cdn.jsdelivr.net/gh/fawazahmed0/quran-api@1/editions/eng-syedvickarahame.json](https://cdn.jsdelivr.net/gh/fawazahmed0/quran-api@1/editions/eng-syedvickarahame.json)

### Sahih al-Bukhari

- **Arabic:** `https://cdn.jsdelivr.net/gh/fawazahmed0/hadith-api@1/editions/ara-bukhari.json`
- **English:** `https://cdn.jsdelivr.net/gh/fawazahmed0/hadith-api@1/editions/eng-bukhari.json`
- **Indonesian:** `https://cdn.jsdelivr.net/gh/fawazahmed0/hadith-api@1/editions/ind-bukhari.json`

### Sahih Muslim

- **Arabic:** `https://cdn.jsdelivr.net/gh/fawazahmed0/hadith-api@1/editions/ara-muslim.json`
- **English:** `https://cdn.jsdelivr.net/gh/fawazahmed0/hadith-api@1/editions/eng-muslim.json`
- **Indonesian:** `https://cdn.jsdelivr.net/gh/fawazahmed0/hadith-api@1/editions/ind-muslim.json`

## API

### `GET /health`

Returns service health status.

### `POST /validate`

Validates LLM-generated text against the embedded corpus.

**Request body:**

```json
{
  "text": "In the name of Allah, the Most Gracious, the Most Merciful",
  "source": "quran",
  "limit": 3
}
```


| Field    | Type      | Required   | Description                                |
| -------- | --------- | ---------- | ------------------------------------------ |
| `text`   | string    | yes        | The text to validate                       |
| `source` | `"quran"` | `"hadith"` | no                                         |
| `limit`  | int       | no         | Maximum number of ranked matches to return |


**Response:**

```json
{
  "query": "In the name of Allah, the Most Gracious, the Most Merciful",
  "matches": [
    {
      "entry": {
        "id": 1,
        "source": "quran",
        "reference": "1:1",
        "text": "...",
        "normalized": "..."
      },
      "score": 0.95,
      "match_type": "fts",
      "similarity": 0.98
    }
  ]
}
```

### `POST /replace-tagged`

Finds `<quran>` and `<hadith>` tags in a block of text, validates the inner content against the embedded corpus, and returns the text with corrected tag bodies.

Use this when your LLM wraps sacred quotes in structured tags and you want to auto-correct hallucinations in place.

**Request body:**

```json
{
  "text": "Allah said: <quran chapter=\"2\" verse=\"255\">wrong verse text here</quran>"
}
```


| Field  | Type   | Required | Description                           |
| ------ | ------ | -------- | ------------------------------------- |
| `text` | string | yes      | Full text containing one or more tags |


**Supported tag formats:**

Quran (attributes are optional):

```html
<quran chapter="2" verse="255">verse text here</quran>
<quran>verse text here</quran>
```

Hadith (attributes are optional):

```html
<hadith collection="bukhari" number="1">hadith text here</hadith>
<hadith>hadith text here</hadith>
```

Hadith `collection` values are resolved to canonical DB names (e.g. `bukhari` → `Sahih al Bukhari`, `muslim` → `Sahih Muslim`).

**Replacement rules:**


| Tag      | When reference attrs are present                                                            | When reference attrs are missing                    |
| -------- | ------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| `quran`  | Lookup by `chapter` + `verse`, pick best-matching edition (score threshold ignored)         | Search by inner text only; empty tag if score < 0.5 |
| `hadith` | Lookup by `collection` + `number`, pick best-matching translation (score threshold ignored) | Search by inner text only; empty tag if score < 0.5 |


- The **entire inner content** of each tag is replaced with the matched canonical text.
- When `chapter`/`verse` or `collection`/`number` are provided, inner citations (e.g. `(QS. 2:255)`) are not parsed — the tag attributes take priority.
- Unrecognized or low-confidence text-only matches leave the tag body empty (`<quran></quran>`).

**Response:**

```json
{
    "text": "Allah said: <quran chapter=\"2\" verse=\"255\">wrong verse text here</quran>",
    "replaced_text": "Allah said: <quran chapter=\"2\" verse=\"255\">Allah! There is no god but He— The Living,— The Self-Sufficient,— The Infinitely Enduring,— Slumber or sleep never reaches Him. All things are His, in the heavens and on the earth. Who is there who can plead in His presence except as He permits? He knows what (appears to His creatures), before or after or behind them. They shall not understand the smallest fragment of His knowledge except as He wills. His Throne extends over the heavens and over the earth, and He does not tire in guarding and preserving them; And He is the Most High (Al-A'li), the Supreme (Al-Azeem, in Glory). [This Holy Verse glorifying Allah is known as Ayat-ul-Kursi]</quran>",
    "replacements": [
        {
            "tag": "quran",
            "original": "wrong verse text here",
            "matched": "Allah! There is no god but He— The Living,— The Self-Sufficient,— The Infinitely Enduring,— Slumber or sleep never reaches Him. All things are His, in the heavens and on the earth. Who is there who can plead in His presence except as He permits? He knows what (appears to His creatures), before or after or behind them. They shall not understand the smallest fragment of His knowledge except as He wills. His Throne extends over the heavens and over the earth, and He does not tire in guarding and preserving them; And He is the Most High (Al-A'li), the Supreme (Al-Azeem, in Glory). [This Holy Verse glorifying Allah is known as Ayat-ul-Kursi]",
            "score": 0.032786885245901676,
            "chapter": 2,
            "verse": 255
        }
    ]
}
```


| Field                     | Type   | Description                                    |
| ------------------------- | ------ | ---------------------------------------------- |
| `text`                    | string | Original input text                            |
| `replaced_text`           | string | Input with corrected tag bodies                |
| `replacements`            | array  | One entry per tag processed                    |
| `replacements[].tag`      | string | `"quran"` or `"hadith"`                        |
| `replacements[].original` | string | Inner content before replacement               |
| `replacements[].matched`  | string | Corrected inner content (omitted when emptied) |
| `replacements[].score`    | number | Similarity score of the chosen match           |
| `replacements[].chapter`  | number | Quran chapter (when known)                     |
| `replacements[].verse`    | number | Quran verse (when known)                       |


**Example (Quran with reference):**

```bash
curl -s -X POST http://localhost:8080/replace-tagged \
  -H "Content-Type: application/json" \
  -d '{"text":"<quran chapter=\"2\" verse=\"2\">In the Name of Allah... (Surah al ikhlas. 1:1)</quran>"}'
```

**Example (Hadith with reference):**

```bash
curl -s -X POST http://localhost:8080/replace-tagged \
  -H "Content-Type: application/json" \
  -d '{"text":"<hadith collection=\"bukhari\" number=\"1\">incorrect hadith wording</hadith>"}'
```

**Example (text-only, no attributes):**

```bash
curl -s -X POST http://localhost:8080/replace-tagged \
  -H "Content-Type: application/json" \
  -d '{"text":"<quran>In the name of Allah, the Most Gracious, the Most Merciful</quran>"}'
```

## Local Development

### Prerequisites

- Go 1.25+
- Raw JSON datasets placed in `data/raw/` (see seeder for expected filenames)

### Build the database

```bash
go run ./cmd/seed -raw data/raw -out data/generated/data.db
```

### Run the API

```bash
go run ./cmd/validator-api
```

The server listens on `:8080` by default. Override with environment variables:


| Variable  | Default                  | Description                 |
| --------- | ------------------------ | --------------------------- |
| `ADDR`    | `:8080`                  | HTTP listen address         |
| `DB_PATH` | `data/generated/data.db` | Path to the SQLite database |


### Build the Docker image

```bash
docker build -t islamic-text-validator .
docker run -p 8080:8080 islamic-text-validator
```

## Deployment

On push to `main`, the GitHub Actions workflow in `.github/workflows/deploy.yml`:

1. Downloads source JSON into `data/raw/`
2. Runs `go run ./cmd/seed` to bake `data/generated/data.db`
3. Builds and pushes the Docker image to Google Container Registry
4. Deploys to Cloud Run with `DB_PATH=/app/data/generated/data.db`

Required GitHub secrets: `GCP_PROJECT_ID`, `GCP_REGION`, `GCP_WORKLOAD_IDENTITY_PROVIDER`, `GCP_SERVICE_ACCOUNT`.